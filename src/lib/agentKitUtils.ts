import { SolanaAgentKit } from 'solana-agent-kit'; 
const agent = new SolanaAgentKit({ 
  network: 'devnet', 
  apiKey: 'YOUR_API_KEY' 
}); 

// figure out how to initialize an agent instance without private key or wallet first

console.log('Token deployed:', tokenAddress); 
